import { Routes } from '@angular/router';
import { ProductPreviewCardComponent } from './product-preview-card/product-preview-card.component';

export const routes: Routes = [
  { path: '', component: ProductPreviewCardComponent },
];
